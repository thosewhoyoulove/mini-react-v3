---
description: JSDoc 在声明上方；函数体内单行注释跟在代码后
globs: "**/*.{ts,tsx}"
alwaysApply: true
---

# 注释约定

- **函数 / 字段 / 类型 / 常量**：用 `/** */` 写在声明正上方，可以多行，把含义写透。
- **函数体单行注释**：先写代码。能一行就同行尾注释；写不下就放在该行下面。不要把 `//` 挡在语句前面。

详见仓库根目录 `AGENTS.md`。
