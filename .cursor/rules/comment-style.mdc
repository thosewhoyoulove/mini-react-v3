---
description: JSDoc 在声明上方；重要函数写步骤总图；函数体内单行注释跟在代码后
globs: "**/*.{ts,tsx}"
alwaysApply: true
---

# 注释约定

- **函数 / 字段 / 类型 / 常量 / 模块级指针**：用 `/** */` 写在声明正上方，可以多行，把含义写透。哪怕只有一行也用 `/** */`，不要用 `//`。类型字段、模块级指针之间空一行。
- **文件级总述**：import 之后、第一个声明之前。画出核心结构（链表头在哪、谁走哪条路径），不要只写「这个文件实现了 XX」。
- **重要函数**（调度、工作循环、commit、链表挂接、mount/update 成对路径）：JSDoc 列出 1. 2. 3. 步骤；函数体逐步标注，编号对齐。
- **函数体单行注释**：先写代码。能一行就同行尾注释；写不下就放在该行下面。不要把 `//` 挡在语句前面。
- **连等赋值**（`a = b = hook`）：拆开再标，不要把两步揉进一行尾注释。

详见仓库根目录 `AGENTS.md`。
